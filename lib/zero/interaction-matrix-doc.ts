/**
 * AUTO-GENERATED — DO NOT EDIT BY HAND.
 * Source of truth: docs/interaction-matrix.md
 * Regenerated by scripts/gen-interaction-matrix.mjs via the predev / prebuild hooks.
 *
 * This file inlines the canonical markdown so the /matrix-interactions web viewer can
 * render it (the page is a static/client component with no runtime fs). Edit the .md,
 * not this file — it will be overwritten on the next dev/build.
 */
export const INTERACTION_MATRIX_MD = "# Zero — interaction matrix (what-triggers-what)\n\nThe single place that answers \"when the user does X to a [kind] entity in [state], what\nhappens (and what's the guard)?\" Use it to check a NEW request against existing behaviour\nBEFORE building — if a request contradicts a CURRENT row, flag it; if it fills a PROPOSED\nrow, cross-check the TODO ledger.\n\nStatus tags: **[CUR]** = built + verified today · **[PART]** = partially built ·\n**[TODO]** = proposed/awaiting decision (see TODO ledger).\n\n> **This is the SOURCE OF TRUTH.** It's a git-tracked repo file. The `/matrix-interactions`\n> web viewer renders it via `lib/zero/interaction-matrix-doc.ts`, which is AUTO-GENERATED from\n> this file by `scripts/gen-interaction-matrix.mjs` (runs on `predev`/`prebuild`). Edit THIS\n> file; never hand-edit the generated `.ts`. The memory doc\n> `v0_memories/user/zero-interaction-matrix.md` is only a pointer here.\n\n## Vocabulary\n- **kinds**: soul · individual · organism · community · space · resource · task · moment · instant.\n- **states** (`getState().word`, one axis, mutually exclusive; kinds.ts): `open` → `complete`\n  → `closed`; plus `ongoing`, `cancelled`, terminal `dead`/`retired`. `open` includes a\n  reopened entity.\n- **axes/fields**: `schedule{startAt,endAt,at,dueAt,duration,engagements[]}` · `closeAt` (frozen\n  close instant) · `closePolicy` ('manual' opts out of auto midnight-close) · `completed` (task done\n  scalar) · `accent` (color) · `taggedContextIds` (multi-parent links) · `createdAt`/`ownerId`/`createdBy`.\n  EFFECTIVE END (`effectiveScheduleEnd`, kinds.ts): declared `endAt`, ELSE concrete `startAt`+`duration`\n  (minutes) ⇒ IMPLIED end. Used everywhere an end matters (getState ongoing/complete, `computeCloseAt`,\n  lifeline bounds `entityInterval`, dayline paint) so start+duration ends at start+duration, never a ghost.\n  Engagements (RENAMED from \"sessions\" v0.4.8) each have `{startAt,endAt?,via?:\"focus\"|\"play\"}` (`via`\n  renamed from \"kind\").\n\n## TRIGGERS\n\n### Navigation\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Drill into (open) an entity | any kind | Records a presence segment (activity log); becomes the breadcrumb leaf | no-op if same id repeated | [CUR] |\n| Left-click an ENTITY CONTENT row (v0.2.151) | any content row (`m`/`s` rung) | drills in ONLY when the click lands on the TITLE TEXT (a shrink-to-content button that underlines on hover); the rest of the row space does nothing on a plain click | title-only. v0.2.152: the whole row is ALSO a drag source (see Drag rows), but a click ≠ drag — a 6px pointer-activation distance means a click without movement still drills / toggles; grip/caret/×/glyph keep their own actions | [CUR] |\n| Hover an ENTITY CONTENT row (v0.2.151) | any content row | a subtle muted band (`hover:bg-muted/40`) tints the whole row so the hovered unit reads clearly; a `cursor-grab` + faint grip-dots hint hint that it's draggable | purely visual; also still lights the matching dayline tick (see Navigation-adjacent hover row) | [CUR] |\n| Drag an ENTITY CONTENT row — SIBLING REORDER (v0.2.152, reworked v0.2.154) | any content row | grab anywhere on the row (`@dnd-kit`, whole-row handle; 6px activation so a click still drills) and move; an accent BAR shows before/after the row the CURSOR is over (above/below its midpoint). On drop the full visible sibling order is rebuilt + persisted via `ctx.reorder`→`reorderContextItems`. Rows spring-SETTLE (motion `layout=\"position\"`); a translucent (`opacity 0.85`) DragOverlay clone follows the cursor | v0.2.154: drop target decided by hit-testing the LIVE cursor Y (`onDragMove` + own row rects), NOT dnd-kit `over` (which lagged a row or two). Drag scoped to one level's siblings (own `DndContext` per Content) | [CUR] |\n| Drag an ENTITY CONTENT row — NEST/reparent into another | any content row | DEFERRED (was briefly live v0.2.152–.153 as a middle-band drop). Planned gesture: DWELL-to-expand — hover a target row long enough while dragging ⇒ it auto-expands so you can drop precisely into its child list. `ctx.reparent`→`moveEntityToContext` (cycle-guarded parentId move) stays in place for it | [TODO] |\n| Drill INTO a web resource | resource w/ `webUrl` (desktop) | mounts a native tab; if already warm/parked ⇒ instant reveal (no reload) | web preview iframes internal `/…`; native only in Electron | [CUR] |\n| Drill AWAY from a web resource | resource w/ `webUrl` (desktop) | PARKS the tab: hidden + background-throttled, kept resident (warm) for instant re-open | LRU pool cap 4 ⇒ oldest parked tab destroyed | [CUR] |\n| Close button (§0/§1 header ×) | any drilled-in entity (`path>1`) | climbs out to parent; for a `webUrl` resource also DESTROYS its tab for good | root has no × | [CUR] |\n| Inline row METAFIELD | any child in ENTITY CONTENT | each row shows a kind-relevant metafield (right-aligned, muted, before the lifecycle token): moment/space → span \"start–end · dur\" / \"since start · dur\" (live) / \"whenever\" / lone \"at\"; instant → its point time; task → \"due …\" when set, ELSE its own start/end span / ongoing / at (a task can be scheduled too, not just due); individual → \"sex · age\"; others → nothing. Time-of-day when same day, else \"MMM D, h:mm\". Derived via `rowMeta(e, now)`, never stored | hidden on narrow (<sm) widths; empty kinds add no gap | [CUR] |\n| Right-click ▸ Hide / Unhide | any child in ENTITY CONTENT | toggles stored `hidden`; row collapses via a long 650ms eased grid-rows slide+fade, siblings renumber | counts/timeline/siblings untouched | [CUR] |\n| Right-click ▸ Show hidden / Hide hidden | current context | VIEW toggle (session-only, resets on nav): reveals hidden + auto-hidden children with a `(hidden)` prefix | not a data mutation | [CUR] |\n| Auto-hide (derived) | closed child | a child closed BEFORE today's 5am day-start collapses from ENTITY CONTENT | computed from `closeAt`, never stored; revealed by Show hidden | [CUR] |\n| THREE-RAIL model on the TODAY dayline (v0.4.0 two-rail → v0.6.21 three-rail; band-grows v0.4.4) | planned span · access session · manual activity | a MIDDLE spine flanked by two rails, framed FUTURE · PRESENT · MANUAL-HISTORY: PLANNED rail (TOP, stacks UP) = DECLARED spans only (`planned` memo); MIDDLE spine = the COLLAPSED-ACCESS leaf-spine (`spine` memo — deepest open focus session at each instant, ONE non-overlapping line, the declarable/correctable ACCESS record); RECORDED rail (BOTTOM, stacks DOWN) = MANUAL activity only, `via` `play`+`mark` (`engagements` memo). Auto FOCUS sessions are NOT on the bottom rail anymore (they ARE the middle spine); ancestor focus sessions exist for state+rollup but aren't drawn (kills the A>B>C>D overlap mud). The pure PRESENCE truth rail (machine-observed, unmodifiable) is a SEPARATE record on the standalone ACTIVITY dayline (`tracks=\"presence\"`), untouched. `bandMetrics(plannedCount, recordedCount, middle)` inserts a `MIDDLE_LANE_H=10` strip centered on the seam; each half = taller rail's content + `RAIL_PAD` | combined lane (`tracks=\"both\"`) only; standalone `tracks=\"presence\"` lane stays one centered band | [CUR] |\n| RECORDED bars (bottom rail) — MANUAL only (v0.6.21) | any subtree entity with `schedule.engagements` that are `via:\"play\"` or `via:\"mark\"` (incl. \"whenever\" playables) | one bar per MANUAL engagement touching today, clipped to the day: OPEN play (no `endAt`) ⇒ running bar at the now-edge + UNKNOWN-END fade (same as an open-ended planned bar — a `startAt:\"whenever\"` entity, which appears ONLY via this session path, reads like Cook); CLOSED ⇒ crisp span, no fade; MARK ⇒ instant glyph. FOCUS sessions render on the MIDDLE spine, not here. **v0.6.34: an AUTO play (`via:\"play\"` + `auto:true`, the ONGOING-ON-ENTER session) is ALSO skipped here** (`sess.via === \"play\" && sess.auto ⇒ continue`) — the recorded rail is DELIBERATE activity only; an entered entity's presence lives on the middle spine (its twin focus session). Only a MANUAL Play lands on the bottom rail | v0.6.22: iterates `getEntitiesWithEngagements()` (was `collectDescendants(ROOT_ID)`, spaces-only, which hid Resource/Task/Moment leaves); key prefix `sess:` routes it to the recorded rail; tooltip appends `· play\\|mark [· ongoing]` | [CUR] |\n| MIDDLE spine bars — collapsed ACCESS leaf-spine (v0.6.21; leaf-fix + no-fade v0.6.22) | any entity with FOCUS engagements (`via` focus / legacy undefined) | `spine` memo: collect all focus intervals across `getEntitiesWithEngagements()` (v0.6.22 — was `collectDescendants(ROOT_ID)`, spaces-only, so a focused Resource/Task/Moment LEAF like a web resource was invisible and the spine drew its parent Space), end=`now` while open (v0.6.24: the whole dayline runs on the shared PER-SECOND clock `useNowSeconds`, the SAME value the now marker reads — so a just-punched leaf shows within ≤1s, memos advance smoothly instead of once a minute, AND an open segment's right edge can never render past the marker; the open segment's START is also clamped to the marker for the sub-second window where a fresh start is a hair ahead of `now`, so the tick sits AT the marker not a few px right — the reported bugs), then FLATTEN by max-start-wins (in each gap the visible owner = the active interval with the greatest start = deepest/current leaf), coalescing same-owner slivers ⇒ ONE continuous non-overlapping line. NO unknown-end fade (v0.6.22: the spine is always \"up to now\" by construction, so a crisp right edge is correct + avoids the fade lag). `track:\"middle\"`, key prefix `spine:`, single lane centered on the seam. ACCESS twin of the pure presence rail; `--sessionStart/End` edits the underlying focus session so the spine slides | [CUR] |\n| PLANNED bar length from --duration (v0.4.8) | an entity with a concrete start + `duration` but NO declared `endAt` | paints a FIXED-length planned tick spanning start → start+duration (via `effectiveScheduleEnd`) and reads as NOT ongoing once now passes that implied end — instead of a ghost bar stretching to now. Set through the create bar / menu `--duration:` (`90` / `1h30m` / `2h` / `2d`) | duration with NO concrete start stays meta-only (DURATION row); a declared `endAt` still wins | [CUR] |\n| LANE PACKING within a rail | 2+ spans overlapping in the same rail | greedy interval packing (`packLanes`): each bar takes the lowest lane whose last bar ended at/before its start; else a new lane opens. TOP packs by START (tiling declared spans share lane 0); BOTTOM packs LONGEST-FIRST (longest engagement hugs the seam) — this SUBSUMES the old ongoing stack (all running engagements share the now-edge ⇒ fully overlap ⇒ one lane each, longest nearest seam). Overflow (v0.4.4; centered-seam v0.4.5) = the WHOLE BAND GROWS TALLER around a CENTERED seam: ticks keep FIXED height (`PLANNED_LANE_H=11`/`RECORDED_LANE_H=10`, no shrink) and HUG the seam (planned stack up, recorded stack down); band height = 2× the taller half (`bandMetrics`) | DEFERRED (larger §3 sizes): (2) bounded lanes + \"+N\" marker; band-grow may later be tied to Face size | [PART] |\n| Presence tick rounding | a run of contiguous presence segments (one \"session of using Zero\") | only the FIRST tick of a run rounds its LEFT corners and the LAST rounds its RIGHT; interior ticks are fully SQUARE so a session reads as one continuous pill. A gap (app backgrounded) starts a new run; an isolated tick is fully rounded | `roundLeft/roundRight` derived on the FULL ordered segment list (`prev.leftAt===enteredAt` adjacency) so it survives pan/clip; applies on every lane that paints presence | [CUR] |\n| Tick opacity (any dayline) | any tick | PRESENCE ticks fully OPAQUE at all times (the solid record of where I was). PLANNED ticks ALSO full opacity now — the dynamic coverage/faint mapping is DISABLED via `DYNAMIC_PLANNED_OPACITY=false` (v0.3.95; machinery kept for flip-back). Root presence = solid theme-bg chip (near-black dark / near-white light) + grey hairline | root fill themed; no color-muting | [CUR] |\n| PAST planned tick opacity (coverage) [DISABLED v0.3.95] | a real-duration planned occurrence fully in the past (end ≤ now) | WHEN `DYNAMIC_PLANNED_OPACITY` ON: opacity reflects how much it was HONORED — fraction of its window overlapped by recorded PRESENCE at that entity or any descendant, mapped LINEARLY between a 0.15 floor (missed) and 0.90 ceiling (fully honored) = `0.15 + coverage × 0.75`. Hover still snaps to full. Currently OFF ⇒ full opacity | one place at a time so segments don't double-count; clamped [0,1]; `coverage` derived in the planned memo, re-runs on activity-log change | [CUR] |\n| Dayline tick HIGHLIGHT (cross-component) | HOVER an ENTITY CONTENT row | every dayline tick whose `id` matches the hovered row's entity grows to 26px + goes fully opaque (animated). Threaded as `highlightId` into both daylines (TODAY combined + ACTIVITY presence) | HOVER-ONLY — the open context is deliberately NOT highlighted (a persistent pin felt stuck); cleared on mouse-leave | [CUR] |\n| §4 PINS frame source (v0.5.0 REPURPOSE; +pinning v0.5.1; +global-scan/timer v0.5.2; +two-list v0.5.4) | the §4 band | TWO horizontal lists around an `ONGOING` label: `[ pinned-idle chips ] ONGOING [ ongoing chips ]`. RIGHT of the label = everything OWN-ongoing via `getOwnOngoingEntities` (scans ALL entities — v0.5.2 fix; `isOwnOngoing`; pinned-and-ongoing counts as ongoing and sits right). LEFT = PINNED entities (`getStarterPinnedEntities`, pin order) that are NOT currently ongoing (idle quick-start shortcuts). A chip that starts/stops CROSSES the label and is FLIP-animated between lists (see FLIP row). Re-scans on `dataRev` + a 1s `TICK_MS`. `zero0-pins.tsx` (`zero0-pinned.tsx` DELETED) | auto-shown when either list is non-empty; the ONGOING label shows only when ≥1 ongoing; pins persist via `starterPins` in UserItems | [CUR] |\n| §4 NOTIFICATION CHIP (v0.6.13; universalized v0.6.14) | ANY entity (pinned OR not) that just STOPPED being ongoing, OR an instant just MARKED | a transient chip LINGERS on the ONGOING side — `NOTIFY_LINGER_MS` 10s for a stopped entity (`getRecentlyEndedEntities`, any kind but instant/soul/recurrence child), `RECENT_MARK_MS` 30s for a marked instant. Glyph reflects the entity's ACTUAL state (`getFaceModel`: filled/done/cancelled/requested), FLASHES filled once on entry (`Zero0Glyph flashFill`, no rotation, `FLASH_MS`) + PULSES gently the whole linger (`.zero0-glyph-pulse`, shallow 1.8s breathe). Glyph is now INTERACTIVE for startable kinds (moment/space/task/…): clicking RESUMES the entity (`onStart` background) → it lifts back into the ongoing list; an instant's mark glyph stays static. Trailing text = FROZEN final session duration (stopped, `formatTimer(lastMs)` from `getRecentlyEndedEntities` — never a `now`-relative string, so it can't flicker to \"00s\" mid-fade) or \"Ns ago\" (marked). A PINNED entity that stops now MORPHS into a notification chip IN PLACE, THEN slides to the pins as a pure FLIP slide (never enters `leaving`, so NO fade) | 1s tick re-reads both feeders; a NON-pinned chip whose window closes drops from the memo → `leaving` → `.zero0-chip-out` fade; a PINNED one falls into `pinnedIdle` (same id stays present ⇒ slide). Honors reduced-motion | [CUR] |\n| §0 DURATION row breakdown (v0.6.13) | §0 (and FULL face) of a non-instant with 2+ tracked engagements, no `--duration` override | reads `[total] · <dur1> (<when1>) · <dur2> (<when2>) …` — the grand total then one segment PER engagement, newest first (`getDurationCells`). In the FULL face each segment is a faint hoverable token whose `title` is the engagement's whole `start – end` (the live one pulses); the plain meta string joins them with \" · \". `null` (⇒ plain total only) for an instant, an explicit `--duration`, or <2 engagements | mirrors `getScheduleCells`' faint/pulse language; `age` (beings) uses the same breakdown row | [CUR] |\n| §4 PINS chip FLIP move (v0.5.4; tick-gated v0.5.7) | a chip changing list (pinned-idle ⇄ ongoing) or shifting position | a dep-free FLIP (`useFlipRow`): last position stored per ENTITY ID (survives React's unmount+remount reparent between the two lists) → after commit measure new position → invert with a `translate` then play back over `FLIP_MS≈480ms` `cubic-bezier(.22,1,.36,1)`. Transform lives on a WRAPPER so the chip keeps its own bg/border transitions. v0.5.7 FIX: the effect is now GATED on a structural `layoutSig` (ordered ids per list) and BAILS on unchanged sig — so the 1s timer tick / focus change no longer re-fire it. Also clears any in-flight transform BEFORE measuring the settled LAST rect. (Bug: running every render meant a tick landing mid-transition stored the interpolated position as baseline and re-animated the residual → chip \"bounced right a little every second, decaying\") | no enter/exit anim (a brand-new ongoing chip just appears; an ended UNpinned chip just drops); only ≥0.5px moves animate | [CUR] |\n| §4 PINS chip live TIMER (v0.5.2; stable-width v0.5.3) | an ONGOING chip | a muted `tabular-nums` timer, updated each second (`ongoingTimer`): a KNOWN end (declared `endAt`, or concrete start + duration) ⇒ COUNTDOWN `-Nm Ss` to the end; NO known end (open engagement or open-ended running span) ⇒ ELAPSED counting up from the open engagement's punch-in (else the concrete span start). Uses a LOCAL `formatTimer` (NOT shared `formatDuration`) that ALWAYS keeps the seconds slot AND zero-pads every non-day unit to 2 digits (`00s`→`59s`, `05m 00s`) — so within a tier the char count is CONSTANT and the chip width never jitters as it ticks; only a rare tier-boundary crossing (→ new leading unit) changes width, smoothed by the FLIP row anim; day-scale drops to `Nd HHh MMm` | idle/pinned-not-ongoing chips show no timer | [CUR] |\n| Click a §4 PINS chip BODY (v0.5.6) | ONGOING chip | FOCUS it ��� `navigateTo` via `openPin`, no engagement write | navigation only (the running engagement/span is untouched) | [CUR] |\n| Click a §4 PINS chip BODY (v0.5.6) | pinned-IDLE chip | START an engagement AND focus it — `openEngagement(id,\"play\")` + `navigateTo` via `startPin(id,true)` | `via:\"play\"` = DELIBERATE + PERSISTENT (not in `focusOpenRef`, so leaving never punches it out; hydrate keeps it running) | [CUR] |\n| ALT/⌘/CTRL-click a §4 PINS chip BODY (v0.5.6) | any chip | START in the BACKGROUND, staying on the current canvas (parallel with anything already ongoing) — `startPin(id,false)`; NO-OP if already ongoing | Alt is the safe modifier (⌘/Ctrl+click = right-click on some platforms); handled in `PinChip.bodyClick` | [CUR] |\n| Click a §4 PINS chip GLYPH (v0.5.6) | any chip | STAY on canvas: ONGOING ⇒ STOP it (`endOngoing` via `endPin` — close its engagement or cap its running span at `now`); IDLE ⇒ START in the background (`startPin(id,false)` = `openEngagement(id,\"play\")`). Glyph SPINS only while ongoing | `stopPropagation` so it doesn't also focus; an ended unpinned chip drops on next scan, an ended PINNED chip lingers (idle) | [CUR] |\n| Right-click a §4 PINS chip | any chip | opens the unified entity menu (`openMenu`) with Pin/Unpin at the top | same menu-model as everywhere | [CUR] |\n| \"Pin\" / \"Unpin\" menu item (ACTIVE v0.5.1) | any entity | top-level toggle (label reflects `isStarterPinned`) that adds/removes the entity from `starterPins` (`toggleStarterPin`), so its chip LINGERS in §4 even when not ongoing. Was the dormant \"Pin as starter\"; RELABELED to plain Pin/Unpin, action ids keep the legacy `starter-` prefix | `starter-pin`/`starter-unpin` canvas-intercepted (not `applyEntityMenuAction`); `openMenu` always passes `starterPinned` so it shows on every entity menu | [CUR] |\n| Engagement storage (all entities) | any entity's clock | ENGAGEMENTS live ON the entity as `schedule.engagements: {startAt, endAt?, via}[]` (RENAMED from `sessions`/`kind` v0.4.8) — the last entry lacking `endAt` is the single OPEN (RUNNING) one. These are the RECORD (what actually happened) and are DELIBERATELY NOT mirrored into scalar `startAt/endAt` (those stay the DECLARED plan — e.g. a Play'd \"whenever\" keeps `startAt:\"whenever\"`) | `getEngagements`/`getOpenEngagement`/`hasOpenEngagement` (pure, kinds.ts); `openEngagement`/`closeEngagement`/`toggleEngagement`/`endOngoing` (writes, data.ts) | [CUR] |\n| Engagement single-instance rule | any entity | at most ONE open engagement at a time — `openEngagement` is idempotent (no-op if one is already open) | — | [CUR] |\n| §4 PINS chip tint (fill v0.5.3; focus-def fix v0.5.5; gentled v0.5.6) | any chip | the entity's own `accent`, else inherited accent from the parent chain, else the bluey Sleep default for sleep-titled, else `--muted-foreground`. Colors the chip border + glyph + a faint accent WASH behind the chip: ~10% `color-mix` normally, a touch STRONGER ~26% when FOCUSED. v0.5.6 REVERTED the v0.5.4 heavy 42%+2px-ring (too loud) back to this gentle level. FOCUSED = \"the chip's entity IS the current canvas context\" (`e.id === focusId`, `focusId = path[len-1]`; the v0.5.5 fix, kept) | `getInheritedAccent` + `sleepDotColor`; `focusId` from the nav path leaf | [CUR] |\n| §4 PINS auto-visibility (v0.5.0) | any load | the band shows ITSELF whenever the union (ongoing ∪ pinned) is non-empty and HIDES (renders null) when empty — no manual toggle. The footer \"pinned\" toggle was REMOVED | driven by the ongoing scan + pin list | [CUR] |\n| §4 PINS list DIVIDER (v0.5.8) | between the two lists | a full-frame-height hairline (`w-px self-stretch bg-border`, `-my-2` to cancel the band padding so it spans edge-to-edge) separates the pinned-idle list (left) from the `ONGOING` label + ongoing list (right) | shown ONLY when BOTH lists are non-empty (nothing to separate otherwise) | [CUR] |\n| §4 empty override (§4 keybinding) | nothing pinned or ongoing | by default the empty band is hidden; pressing §4 (`frequent` flag → `forceShow`) force-reveals the empty frame with a muted \"nothing pinned or ongoing\" placeholder, so you can confirm the frame toggles even when idle | `frequent: false` default in zero0-chord; the §4 digit still maps to it; footer link gone | [CUR] |\n| ANY �� FRAME appearing / disappearing / resizing (v0.6.12) | any hideable frame (§4 pins, ACTIVITY, TODAY/agenda, §1 zero-header) toggling open/closed | ONE unified `<Zero0Frame open>` wrapper: a single 0.8s move where the frame's HEIGHT (grid-rows 0fr↔1fr) and its content OPACITY animate TOGETHER (ease-in-out), so the frame fades in/out WHILE the rest of the stack slides to fill. Replaces the per-frame `duration-300` height-only collapses AND the v0.6.11 §4 two-phase. Always-mounted; `inert` while closed; honors `prefers-reduced-motion` (static) | `components/zero0/zero0-frame.tsx`; §4 `visible = presentTotal>0 ‖ forceShow`. Distinct from the per-CHIP 1.2s fade (v0.6.10) — chips fade within a shown frame, the frame fades as a whole | [CUR] |\n| `--start:<time>` on an ONGOING entity (v0.6.11, fixed v0.6.12) | the CURRENT entity, has an OPEN engagement | BACKDATES the running session — `setOpenEngagementStart(id, epoch)` moves the open engagement's `startAt` to the new time and SWALLOWS every closed dwell session inside `[newStart, now]` into the one extended open session (a straddling session merges by pulling `start` back to its own start; marks + sessions before the span untouched). v0.6.11 clamped `> prev end` which snapped a backdate forward past the dwell history (barely moved) — now it absorbs them. Logs `engagementStart`. End/at/due still target the schedule; clearing start still clears the anchor | `hasOpenEngagement(ent)` gate in the create-field `start` case; concrete (non-clear) start only | [CUR] |\n| Color picker free-text (create-bar `--color` + entity \"Set color\" menu) | any color choice | type a hex (`#8b5a2b`) OR a CSS name (`brown`, `grey`); resolved to hex via the browser parser; invalid text can't commit | shares the swatch `color:<hex>` path | [CUR] |\n| Click the minimized TODAY band (v0.4.6) | TODAY (agenda) frame, minimized | LEFT-click on EMPTY band area (anything that isn't a `[data-barkey]` tick) MAXIMIZES the frame; the title collapses/expands with the grid-rows 0fr↔1fr animation. Tick clicks still open their entity (guarded by the `closest(\"[data-barkey]\")` check) | agenda frame only; right-click still opens the frame menu (minimize/maximize) | [CUR] |\n| Chevron in the maximized TODAY title (v0.4.6) | TODAY (agenda) frame, maximized | a chevron button (top-right of the \"today\" title) MINIMIZES on a single click; the title is ALWAYS MOUNTED and collapses via grid-rows so both directions animate smoothly, `inert` while collapsed | `onToggleMinimize` from canvas flips `minimized.agenda` | [CUR] |\n| LAST FOCUS restored on load (v0.4.6) | app open (Electron + web) | the drill-in `path` persists to `zero:root-path:v1` (renderer localStorage) on every change; on mount it's restored + VALIDATED against the hydrated store (keep ROOT then each still-live id, stop at first deleted) so you resume exactly where you left off | own key, isolated from the entity store/activity log; falls back to root on malformed/absent storage or a fully-deleted chain | [CUR] |\n| Being the focused context | any kind | opens a focus session ~immediately (dwell now 0, see next row) | — | [CUR] |\n| PRESENCE in the active path — EVERY kind incl. ROOT (v0.6.19; was task/space/resource v0.6.17, all-but-root v0.6.18) | any not-closed entity on the `path` | after `DWELL_MS` (**0** — presence counts immediately), `openEngagement(id,\"focus\")` on EVERY entity root→current — INCLUDING ROOT: its perpetually-open focus session IS your overall Zero USER SESSION, recorded on the activity rail. Records ACCESS = \"how long I've been on this\" (BOTTOM rail), fully decoupled from the OCCURRENCE (top rail). STATE (v0.6.32): a `focus` session NEVER flips state for ANY kind — ongoing is now `play`-ONLY (`ongoingOpenEngagement` returns the open `play`). So presence RECORDS without spinning; the spin comes from the separate AUTO-PLAY rail (next row) | punch-OUT immediate on leaving; `MIN_SESSION_MS` (1.5s) discards a fast pass-through; presence is LIFECYCLE-INDEPENDENT (done/closed entities STILL accrue focus while viewed, v0.6.31); hydrate-cleanup closes EVERY dangling focus (not just the last entry) on ALL kinds across reloads; only tracks what the canvas opened (`focusOpenRef`) | [CUR] |\n| AUTO-PLAY on enter ��� ongoing-on-entering (v0.6.32; `auto` flag v0.6.34) | task, resource, space (`canAutoPlay` = these kinds AND not done/closed/cancelled) | alongside the focus punch-in, the dwell effect auto-opens a `play` session STAMPED `auto:true` (`openEngagement(id,\"play\",now,{auto:true})`) so the entity SPINS + DURATION counts while you're in it. Leaving closes it (DURATION freezes; ACCESS keeps ticking from focus). Tracked in `playOpenRef`. Re-entering an UNDONE task auto-resumes (also `auto`); a DONE task is presence-only (`canAutoPlay` false — ACCESS ticks, no resume) | v0.6.34: `auto:true` keeps it OFF the recorded (bottom) rail + OFF the OCCURRENCES \"N times\" tally (entering is not deliberate activity/occurrence) while STILL counting DURATION. moments/instants/beings never auto-play; a MANUAL play (Play-from-afar, no `auto`) is not in `playOpenRef` so it survives navigation until you drill in (then adopted) | [CUR] |\n| TASK glyph click — state-dependent tree (v0.6.32) | task (routed via `onToggleDone`/`toggleDone`) | SPINNING (open `play`) ⇒ **Stop** (`closeEngagement(id,\"play\")`, drop from `playOpenRef`; focus/ACCESS keeps ticking). RESTING + undone ⇒ **Done**. DONE ⇒ **un-done** — and if still on `path` & `canAutoPlay`, immediately re-open `play` so it resumes spinning in place (dwell won't re-fire on unchanged path). So spinning→done is TWO clicks (Stop, then Done) | keeps focus session across done (v0.6.32 fix: done closes only `play`) | [CUR] |\n| Mark a Task Done while it has an open PLAY session | task with open play (spinning) | `setEntityCompleted(true)` closes only the `play` session (a Done task isn't ongoing) but KEEPS the `focus` presence session open — ACCESS keeps counting while you view it (v0.6.32 fix; was closing focus too, freezing the clock) | done also gates on task-children (see completeSince) | [CUR] |\n| ONGOING glyph rotation | any entity reading `ongoing` (open engagement, or concrete-started moment/space) | glyph rotates clockwise at a calm `SPIN_MS=2345`ms/turn (v0.4.9; was 2340); angular velocity EASES IN at start (playbackRate ramp over `EASE_MS=650`) and decelerates OUT to the nearest upright at stop, so it never snaps on/off. No opacity change. Same for every kind | Web Animations API in zero0-glyph.tsx; pivots the shape's visual centroid; skipped under prefers-reduced-motion | [CUR] |\n| GLYPH MORPH (dep-free radial sampling; v0.6.2) | any MORPHABLE kind (task·space·resource·moment·instant·community·organism) | each convex silhouette is sampled as a RADIAL signature — `MORPH_N=48` radii from box centre, from 12-o'clock clockwise — so morphing = a top-aligned element-wise lerp of two radius arrays (`buildPoints`/`lerpRadii`). While a morph is ACTIVE the crisp `KindShape` is swapped for a `<polygon>` whose `points` are written each frame by an rAF (no per-frame React state). TWO drivers: (a) KIND CHANGE ⇒ one-shot ease `MORPH_MS=380` from the previous kind's shape into the new one; (b) SPACE ONGOING ⇒ a periodic hexagon→square→hexagon FLOURISH (rest at hexagon, brief SYMMETRIC `sin(x·π)` there-and-back dip toward a SUBTLY smaller square (`SPACE_MORPH_SQUARE_SCALE=0.95` of the true square radii, so it pinches in just a touch — v0.6.6) in the last `SPACE_MORPH_PULSE=68%` of each `SPACE_MORPH_PERIOD_MS=1345` (v0.6.6: one flourish every 1.345s, dip held ~915ms), no dwell — hex→square and square→hex are EXACTLY equal length (sine symmetric about its mid); v0.6.4 tuned: more frequent while holding the dip ~924msrequent + slower morph, ~924ms) layered over the spin | corners approximated during motion only (static glyph stays pixel-identical crisp); non-morphable kinds (individual \"Z\" stroke, soul dot, requested-task pennant) just swap; skipped under prefers-reduced-motion; needs the same glyph instance to persist across the kind change | [CUR] |\n| Glyph click on a MOMENT/SPACE — MANUAL PLAY = bottom rail (v0.6.26; unified moment+space, corrects v0.6.18/.23/.25) | moment, space | Play is a MANUAL PLAY, exactly like Playing a whenever/pinned entity: idle → **Play** = `openEngagement(id,\"play\")` (a `via:\"play\"` session → BOTTOM/recorded rail); running-play → **Stop** = `closeEngagement`. A moment is NO LONGER \"its own occurrence\" — its scheduled time is PLANNED-only (top rail, user-set), and PLAYING records actual time on the bottom rail, same as a space. NEVER writes scalar `startAt`/`endAt` ⇒ no phantom top-rail tick / fake countdown (the Edan bug). NOT the auto focus/presence MIDDLE spine — DECOUPLED. Reads `ongoing` + spins via the play session; §0 PLAYED row derives from it. A manual play is a STOPWATCH: survives navigation, runs until you Stop it (never auto-punched-out). `startOccurrence`/`endOccurrence`/`reopenOccurrence` + `occurrenceAction` are RETIRED as the Play mechanism | canvas `togglePlaySession` (via-aware: toggles only `via:\"play\"`, NOT `focusOpenRef`); menu label + `applyEntityMenuAction` key off the play session (viewing still reads \"Play\"). SINGLE SLOT per entity: a manual play while a focus session is open (you're viewing it) is a no-op — presence already tracks you; the dwell effect skips punching focus atop an open play (entering a played entity doesn't stop the stopwatch). Two simultaneous focus+play = deferred (two-open-by-`via`, see todos) | [CUR] |\n\n### Lifecycle (right-click menu / create-bar directives)\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Mark as Done | task, not-ended | sets `completed`; for OWNER also completes + stamps `closeAt` (next local midnight) — BUT only if all `kind===\"task\"` children are complete (else \"done but open\", see below) | only kinds with `hasDoneState` (task); manual closePolicy skips the stamp | [CUR] |\n| Mark Done with a still-open task child | task, done, has undone task-child | reads \"done but open\": keeps the Done checkmark, glyph NOT filled, state word = **`done`** (NEW dedicated word, ranked above ongoing/open; NOT `complete`); does NOT stamp `closeAt`. AUTOCOMPLETES (→ `complete`) the instant every `kind===\"task\"` child completes. Non-task children never gate | `allTaskChildrenComplete` in completeSince; `getStateInner` returns `{word:\"done\"}`; `childBreakdown`/`formatState` handle it | [CUR] |\n| Play / Stop / Reopen (menu OR glyph click) — MOMENT/SPACE (v0.6.26) | moment, space | Play → `openEngagement(id,\"play\")`, Stop → `closeEngagement(id,\"play\")` (canvas intercepts both → `togglePlaySession`; `applyEntityMenuAction` is the non-canvas fallback). Reopen → `reopenEntity` (just un-closes; nothing to archive since Play never writes the scalar anymore). Drives the BOTTOM rail (a `via:\"play\"` session), NEVER the scalar `startAt`/`endAt` | v0.6.32: menu label keys off `hasOpenEngagement(e,\"play\")` (play-specific — a concurrent `focus` presence session no longer masquerades as \"playing\"); Stop/close is via-scoped to `play` so presence keeps ticking | [CUR] |\n| §0 rows — plan/actual clocks (v0.6.28) | task, moment, space, resource (planned kinds) + any engaged kind | PLANNED START / END / DURATION = pure-planning scalars (`startAt`/`endAt`, user-set only, top rail), ALWAYS shown for every `isPlannedKind` (task·moment·space·resource; v0.6.31 — a minimum planning affordance, \"— (none scheduled)\" when unset). DUE = deadline (distinct from planned END). ACCESS = Σ `via:\"focus\"` sessions (PRESENCE, middle rail) — LIFECYCLE-INDEPENDENT (v0.6.31: keeps counting even on a done/closed entity while you view it; getState still ranks complete/done ABOVE session-ongoing so it won't resurrect). DURATION (v0.6.33) = the ACTUAL ongoing-time clock = UNION of ongoing intervals (`play` sessions + a moment/space's in-progress occurrence), live-counting the open play; rich sum+span breakdown, the live span pulsing. DISTINCT from PLANNED DURATION (plan-span) and from ACCESS (presence — can tick while NOT ongoing, e.g. a done task you view). OCCURRENCES (v0.6.33) = COUNT + PLANNED-LIST (\"how many / which\"), NOT a time sum: `<N> times — <planned occurrence list with matched/missed>` (the standalone \"planned occurrences\" row MERGED in here). Clocks never merge; presence (ACCESS) is NOT folded in | `isPlannedKind` (kinds.ts) gates planned rows + `getScheduleCells`; `getPlannedDurationMs` = plan-only; `getOngoingDurationMs/Cells(e,now)` = union (mergeIntervals) of play+occurrence; `getSessionMs/Cells(e,now,via?)` core (getAccessCells=focus); OCCURRENCES count = play sessions w/ `endAt!==startAt`; instant keeps its own plain mark-tally occurrences row | [CUR] |\n| §0 PLANNED OCCURRENCES + status resolver (v0.6.30, Stage B slice 2) | task, moment, space, resource | A dedicated PLANNED OCCURRENCES row: the plan's intended spans (primary scalar span ∪ `occurrences[]`), occurrence-LED with a DERIVED status word each (v0.6.31, NO leading count summary): `Mon 1–2pm (matched) · Fri (upcoming) · Sat (missed)`. Status is PURE + order-independent: `cancelled` (stored, the ONLY stored status — a struck-out attempt), else `fulfilled`→display **\"matched\"** (∃ session focus-OR-play overlapping the window; \"matched/missed\" pair, avoids colliding with a Task's DONE), else `missed` (`now>end && !fulfilled`), else `upcoming`. Shown only when it adds over PLANNED START/END: >1 occurrence OR a missed/cancelled (a lone matched/upcoming primary is suppressed — a fresh task auto-matches its own window). v0.6.33: the standalone \"planned occurrences\" row is GONE — this list is now the tail of the OCCURRENCES row (`<N> times — <list>`) | `occurrenceStatus(occ,sessions,now)` + `getPlannedOccurrences(e)` + `getPlannedOccurrenceRow(e,now)` in face-model (called from the OCCURRENCES row builder); `cancelled` added to `Schedule.occurrences[]` (types); `setOccurrenceCancelled(id,index,cancelled?)` = the write op. TODO(next): a menu/UI to cancel a specific occurrence + multi-span create authoring (\"Mon 1-2pm AND Fri whenever\") | [PART] |\n| §0 START/END/DURATION decoupled from ACCESS (v0.6.19) | §0 face of any kind | START/END rows (`getScheduleCells`) read the OCCURRENCE ONLY — cell 0 = scheduled `startAt`/`endAt` (END pulses `ongoing` when started-not-ended), then faint archived `occurrences[]` as fixed past ticks. NO engagement cells merged in. DURATION = OCCURRENCE length only (`getOccurrenceDurationMs`); a merely-viewed \"whenever\" moment reads \"—\". NEW **ACCESS** row (`getAccessMs`/`getAccessCells`) surfaces accumulated presence + per-session breakdown (the live one pulsing) for any engaged kind | two clocks, never merged: occurrence (top rail, \"when it happened\") vs access (bottom rail, \"how long I was on it\"). Fixes the seed Moment reading START \"11:15 AM\" borrowed from its focus session | [CUR] |\n| `--start`/`--end`/`--at`/`--due` ⇒ OCCURRENCE (v0.6.19) | create-bar directive, self-field mode (inside an entity) | ALWAYS set the top-rail schedule via `setEntityScheduleField`. Removed the old \"backdate the running stopwatch\" shortcut (it would hijack --start on every viewed entity now that all hold a focus session). So `--start:1600` schedules the occurrence for 4pm ��� a top-rail tick, faded-right (`unknownEnd`) while it has no end | occurrence vs access split; to adjust a session use --sessionStart/End below | [CUR] |\n| `--sessionStart` / `--sessionEnd` ⇒ ACCESS session (v0.6.19) | create-bar directive, self-field mode, entity with an open engagement | the DELIBERATE access-session counterpart: `--sessionStart:30min ago` backdates the running engagement (`setOpenEngagementStart`, \"I opened this now but I've been on it 30min\"); `--sessionEnd[:time]` closes it (`closeEngagement(id,undefined,at)` — via undefined ⇒ the last-open of any rail, empty ⇒ now). Targets the BOTTOM/activity rail, never the schedule. Accepts HHMM / \"30min ago\" / \"in 2h\" / now (`parseDateToken`) | errors if no running session here; parser `relFields` lists `sessionstart|sessionend` BEFORE `start|end` so the longer alternation wins | [CUR] |\n| Mark (menu OR glyph click) | markable INSTANT, not complete-under-hard-cap | `markInstant` appends a ZERO-LENGTH engagement (`endAt===startAt`, via `\"mark\"`) — an OCCURRENCE toward the tally. GLYPH SPINS ONCE + FLASHES its fill to 100% at exactly the spin midpoint then back to outline (v0.6.12 `spinOnce` + overlay flash); the occurrence that COMPLETES fills for good. §0 OCCURRENCES row reads \"<count> / <maxNb>\" + timestamps (DURATION row hidden for instants). Marking anchors `closeAt` on the latest occurrence (auto-files next midnight unless `--close:manual`) | `isMarkable` = instant AND not closed AND (not hard-capped-complete). §4 mark chip glyph is STATIC FILLED + non-interactive (only the chip body opens); v0.6.9 | [CUR] |\n| WEBSITE resource glyph always FILLED (v0.6.16) | any `resource` carrying a `webUrl` (the contextual-browser diamond) | its glyph reads FILLED (solid) regardless of lifecycle state — a live reachable page is a \"present, complete\" thing, not an open to-do. Independent of open/closed/complete; applies everywhere the glyph is drawn (§0, rows, chips) since it flows through the one `fillsGlyph` source | short-circuit at the top of `fillsGlyph` (kind===\"resource\" && webUrl). FUTURE: gate on the site being live / not a 404 — deferred | [CUR] |\n| Instant OCCURRENCE / maxNb model (v0.6.12) | INSTANT | an instant COMPLETES (fills, state `complete`) once it accrues its MAX authorized occurrences: marks + a passed scheduled `at` (which counts as ONE). Default `maxNb` 1 = a UNIQUE occurrence (completes when `at` passes or on first mark). `--maxnb:3` requires 3 (soft — extra marks still recorded); `--maxnbhard:3` = HARD cap (no marks past complete). STATES: open (no schedule) · scheduled (`at` set) · complete · closed · cancelled — never ongoing (a point). Instants file at next midnight like moments unless `--close:manual` | `getInstantOccurrences`/`getInstantMaxNb`/`isInstantMaxNbHard`/`completeSince`/`computeCloseAt` in kinds.ts; `setInstantMax` in data.ts; `scheduled` StateWord | [CUR] |\n| Set instant time (`--start`/`--end`/`--at`, or menu Set to now) | INSTANT | v0.6.10: an instant is a ZERO-DURATION POINT — ANY concrete time collapses `startAt === endAt === at` to that one epoch (logs a coincident open+close pair), so it renders as a dayline POINT and NEVER reads as an open-ended ongoing span. A concrete `at` makes state read `scheduled` (v0.6.12). Clearing removes all three | instant normalization in `setEntityScheduleField` + create path + recurrence resolver | [CUR] |\n| Mark as Undone | task, done | clears done + `closeAt` | — | [CUR] |\n| Close / Retire / End | fillsWhenClosed or terminal kinds, not-ended | stamps `closeAt` = now-ish; state ⇒ closed / retired / dead | label by `terminal` meta | [CUR] |\n| Cancel | closeable, not-ended | state ⇒ cancelled | HIDDEN for `individual` | [CUR] |\n| Reopen | any ended | clears close/cancel ⇒ open | only shown when ended | [CUR] |\n| Send / Unsend request | task | toggles `requested` (glyph gains a \"sent\" tail) | task only | [CUR] |\n| Start now / End now | moment, not-ended | stamps `schedule.startAt` / `endAt` = Date.now() | not-ended only; no cross-midnight adjust (simple) | [CUR] |\n| Set to now | instant, not-ended | stamps `schedule.at` = Date.now() | instant only | [CUR] |\n| Set color… (submenu) | any kind | `setEntityAccent(id, hex\\|null)`; paints ticks + row | Clear returns to inherited/neutral | [CUR] |\n| Change into… | creatable kinds | `changeEntityKind`; may stamp `closeAt` if new kind time-closes | identity kinds excluded | [CUR] |\n| Delete | any user/seed | removes (user) or tombstones (seed) | — | [CUR] |\n\n### Time / automatic\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Reaching `closeAt` | completed task / ended-end moment | state flips complete ⇒ closed at the frozen instant | `closePolicy:manual` ⇒ never (rests complete/ongoing) | [CUR] |\n| Past start, no end | moment OR space with CONCRETE startAt | state ⇒ `ongoing`; glyph spins (pivots true centroid). Space now joins Moment (a live container) | end set ⇒ leaves ongoing; `\"whenever\"` is NOT concrete ⇒ stays `open` until played | [CUR] |\n| Open session present | any entity | state ⇒ `ongoing` purely from `hasOpenSession` ��� highest-priority ongoing branch, view-independent (covers Task focus AND Whenever play) | closes when the session closes | [CUR] |\n| CLOSING an entity ends its open engagement (v0.4.2) | any entity with an OPEN engagement that gets Done / Cancelled / Closed | the close writers (`setEntityCompleted`/`setEntityCancelled`/`setEntityClosed`) call `closeSession(id, now)` on transition-to-closed — the engagement's `endAt` is stamped at the close moment (RECORD side, NOT declared `endAt`), so the bar stops printing as ongoing AND opening the entity shows WHEN it ended. HYDRATE cleanup closes pre-existing ORPHAN engagements on any already-closed entity (`isClosed`, any kind incl. `play`), ending at `lastLogAt` | skipped on un-check/restore/reopen (a reopened entity may run again); sub-`MIN_SESSION_MS` spans dropped. Distinct from the focus-cleanup that keeps a `play` stopwatch running while the entity is still OPEN | [CUR] |\n| CLOSED occurrence, no declared end — dayline clamp (v0.4.3) | a CLOSED Moment/Space with a `startAt` but no `endAt` and NO engagement (so v0.4.2's `closeSession` can't reach it) | the `planned` memo no longer marks a CLOSED occurrence `ongoing`; lacking an `endAt` it terminates the bar at `computeCloseAt` (the RECORD close time) instead of stretching to now. A close time before the visible window ⇒ the bar simply doesn't render (correct — it ended earlier). SECOND, independent ghost-bar source alongside v0.4.2 (engagement-less) | `isClosed(occ)` + `computeCloseAt(occ)` in the memo; declared `endAt` still wins when present | [CUR] |\n| ONGOING ROLLUP (v0.4.0; STOPS-AT-BEING v0.4.9) | any CONTAINER (task/space/moment/instant) with a CONTAINED descendant that is ongoing | state ⇒ `ongoing` (lowest-priority ongoing branch, below own open-engagement / own concrete-span): a Space spins while anything CONTAINED inside it runs. Follows `parentId` CONTAINMENT ONLY — a merely TAGGED-in ongoing entity does NOT light up its tagger. Recurses the subtree, short-circuits on first ongoing descendant (`containedOngoingSince` + `_containedResolver`, separate from `getChildren`) | STOPS AT THE FIRST BEING (`isBeing`: soul/individual/organism/community): a being is never made ongoing by what it contains — it reads ALIVE/present, not \"in progress\". Fixes the v0.4.0 `parentId!==null` guard, which failed for the root Individual (whose parent is the Soul, so it wasn't excluded and wrongly spun whenever ANY descendant ran). Complete/done/cancelled keep their word (rollup ranks below them) | [CUR] |\n| Reload with a dangling session (v0.6.20 LIVENESS rework) | any entity | hydrate reconciles a dangling open FOCUS session via `getLastKnownAlive()` (NO heartbeat — reuses the presence log's last visibility/pagehide flush stamp): alive within `ALIVE_GRACE_MS` (60s) ⇒ a mere RELOAD/deploy while still present ⇒ LEAVE IT OPEN (continuous; `openEngagement` idempotent so the canvas re-uses it) → one long ongoing bar. Genuine gap ⇒ close at last-known-alive (NOT `lastLogAt`, which is ≈0 for an idle session, so the OLD code truncated→dropped it → the \"lost 5h\" bug), drop if ≤ `MIN_SESSION_MS`. PLAY stopwatches LEFT RUNNING; NEVER auto-end (correct retroactively via `--sessionEnd`) | `getLastKnownAlive` in activity-log.ts; `ALIVE_GRACE_MS` = open-item #4 (tune while dogfooding) | [CUR] |\n| Bare `HHMM` time entry | create-bar | resolved into the 5am→5am logical day-window containing now | 6/10-digit tokens keep absolute date | [CUR] |\n\n### Create-bar grammar (see zero-create-field.md for full detail)\n| Trigger | Effect | Status |\n|---|---|---|\n| No `--`/kind hint | default kind = **moment**; title verbatim (no verb inference) | [CUR] |\n| `--due` / `--start`/`--end` / `--at` | kind ⇒ task / moment / instant | [CUR] |\n| `--start:now` etc. | stamps Date.now() | [CUR] |\n| `--end: 5min ago` / `--start: in 2h` | RELATIVE offset from now — `<dur> ago` (past) / `in <dur>` (future); `<dur>` = full duration grammar incl. word units (`5min`, `2 hours`, `1h30m`). Captured WHOLE despite the space (dedicated parseEntry passes before the single-token pass). E.g. ends an ongoing entity at now−5min | [CUR] |\n| `--start:whenever` | marks the entity PLAYABLE — `startAt: \"whenever\"` (a trackable thing with no fixed clock time; glyph/menu offer Play/Stop). Only valid on `--start` | [CUR] |\n| `--<digits>-<digits>` | span shortcut (= `--start:X --end:Y`; date-only end ⇒ +2359) | [CUR] |\n| cross-midnight span | moment `endAt<=startAt` ⇒ end +24h | [CUR] |\n| title contains another entity's title | auto-tag via `taggedContextIds` (+inherit its accent unless `--color`) | [CUR] |\n| `--close:manual\\|auto` | sets `closePolicy` (owner-only) | [CUR] |\n| `--duration:<len>` | sets explicit `schedule.duration` (MINUTES) on ANY kind, independent of start. Accepts `1h30m` / `90` (bare = min) / `2h` / `45s` / `2d` / `1:30`; empty clears. Does NOT change inferred kind | [CUR] |\n\n### Display / derived (not stored)\n| Field | Rule | Status |\n|---|---|---|\n| DURATION / AGE (§0 row) | EXPLICIT `schedule.duration` (via `--duration`) wins for ANY kind regardless of start; ELSE: instant ⇒ 0s; HAS SESSIONS ⇒ Σ session spans (open one live to now) — the tracked-time source for a \"whenever\" playable thing; start+end ⇒ span; ongoing (concrete start, no end) ⇒ now−start (live); BEING (individual/organism) with no span/sessions ⇒ now−`createdAt` (AGE); ELSE ⇒ \"—\". A merely-open \"whenever\" moment/space with NO session reads \"—\". Label = **AGE for individual/organism**, else DURATION. Up to THREE adjacent units incl. mo/y | [CUR] |\n| START / END rows (§0; hybrid inline v0.5.8; column-paired v0.5.9; RICH v0.6.0) | shown ALWAYS for MOMENT and SPACE (both span-bearing). HYBRID INLINE: each row LEADS with the SCHEDULED value (or `— (none scheduled)` — keeps the row's PLANNED-intent meaning), then folds in the TRACKED sessions newest→oldest, COLUMN-PAIRED (Nth start above Nth end, same engagement; nbsp-padded monospace). v0.6.0: built from structured `getScheduleCells` (peer to `getFaceMetaRows`, which flattens the same cells to a plain string for block subsets / whole-row title / aggregate). The FULL face renders them RICHLY: CLOSED session cells are FAINT (`text-muted-foreground opacity-70` — v0.6.1 deepened) vs the foreground scheduled prefix; the OPEN session's BOTH cells are NOT faint (full-opacity foreground; v0.6.2 un-fainted the START cell too so the whole live pair reads active) and BOTH cells PULSE (v0.6.8: the start timestamp joins the `ongoing` end so the live pair breathes together — was END-only in v0.6.1; opacity-only `.zero0-pulse`, motion-safe); each cell carries its own seconds-precision `fmt` `title` (per-value hover, replacing the old whole-row tooltip); the row scrolls HORIZONTALLY (`.no-scrollbar`, silent) to reach old sessions, and START/END share a SYNCED scrollLeft (ping-pong-guarded) so columns stay paired while scrolling. Day-aware stamps auto-gain a date next day (`fmtShort`). Instant shows \"at\"; task \"due\"/condensed \"scheduled\"; other kinds only when set | [CUR] |\n| STATE row when ONGOING (§0; v0.6.1; icon v0.6.2) | any span-bearing entity currently ongoing | the FULL face renders a small PULSING \"live\" DOT (solid filled circle — the live/active indicator; v0.6.2 replaced the v0.6.1 rotating ring which read as \"loading\") + the PULSING word `ongoing` (`.zero0-pulse`), then \" · since …\" plain. Detected by the `ongoing` prefix `formatState` emits; every other state renders as the plain string row | motion-safe (pulse honors reduced-motion) | [CUR] |\n| Dayline planned tick | ongoing entity (startAt, no endAt) draws start→now (openEnded, as if end=now) until a real end exists; label \"· ongoing\" | [CUR] |\n| Dayline SESSION coalescing (v0.5.9) | an entity's `schedule.engagements` on the recorded rail | DISPLAY-ONLY merge: sessions walked oldest→newest, any whose gap from the current run's end ≤ `SESSION_MERGE_GAP_MS` (60s) fold into ONE bar (an open session extends the run to now + seals it). Keeps a burst of quick stop→restart toggles (tests/mis-clicks) reading as a single continuous block instead of fragmenting the rail into extra sub-lanes; a merged bar's tooltip appends `· N sessions`. STORED engagements untouched (§0 lists them all) | genuinely separate sessions (>60s gap) stay distinct bars; presence rail unaffected | [CUR] |\n| Dayline UNKNOWN-END FADE (v0.5.1; shortened v0.5.3; sessions v0.6.7; ONE-ELEMENT v0.6.20) | a tick with NO declared/implied end, not closed (`unknownEnd`, not a point/mark ⇒ `fading`) — a PLANNED tick with a concrete start, OR an OPEN recorded SESSION run | the fade is now a `mask-image` on the SAME ONE element (v0.6.20 rework — was a separate sibling `<div>`, which caused a hover/timing mismatch + a rounded SEAM near now). The tick is LEFT-anchored, its width = `start→now (widthPct) + UNKNOWN_END_FADE_PX`, and a `linear-gradient(#000 calc(100%−FADE), transparent)` mask fades the last FADE px — taking the fill AND the hairline border with it (no crisp border box). Rounding is `rounded-l` only (the \"continues\" right edge stays open). Solid/fade boundary lands exactly on now. ONE hover target, ONE transition | a closed tick, a real instant/point, or any bar with an effective end never fades; on the recorded rail ONLY the single OPEN run fades (closed runs stay crisp), which is what makes `startAt:\"whenever\"` entities (drawn only via sessions) trail a fade like an open-ended plan | [CUR] |\n| Dayline tick vertical align | single-track lane ⇒ ticks centered (top:50%). COMBINED lane (v0.6.21 THREE-RAIL) ⇒ a MIDDLE spine strip (`mid=MIDDLE_LANE_H`) centered on a DYNAMIC seam, flanked by two rails: PLANNED lane 0 hugs just ABOVE the strip (band edge = seam−mid/2, higher lanes stack UP), RECORDED lane 0 hugs just BELOW it (seam+mid/2, higher lanes stack DOWN), MIDDLE is the single spine lane centered ON the seam. FIXED heights, via `-translate-y-1/2` off a numeric `railTop`; `laneGeom(rail, laneIndex, seam, mid)`. WHOLE band grows so the spine stays centered (`bandMetrics(...,middle=true)` ⇒ `bandH = half*2 + mid`). The day label centers across the whole top half and stays anchored as the band grows | [CUR] |\n| Website content zoom (desktop) | trackpad pinch OR ⌘/Ctrl + scroll OR ⌘/Ctrl + = / - / 0 over an embedded web resource | steps that resource's `WebContentsView` zoom factor (0.3–5×, 0.1 steps; 0 resets to 100%). Per-tab, so each resource keeps its own scale. Electron-only (`zoom-changed` + `before-input-event` in main.cjs) | [CUR] |\n| Entity-content row color dot | a small dot before the title ONLY when the entity has its OWN `accent` (manual/`--color`/menu). Inherited ancestor color NOT painted | [CUR] |\n| §0 COLOR meta row | shown only when `accent` set; dt cell paints the swatch, value = raw hex | [CUR] |\n| TAGS / TAGGED BY (§0) | forward links + derived reverse (who points here) | [CUR] |\n\n## Maintenance\n- On any change to getState / menu-model / create-parse / canvas behaviour, UPDATE the\n  affected row(s) and bump the Status tag. Add a row for every NEW trigger.\n- Keep effects one-line; link deep detail to the relevant topic doc.\n- After editing, the `/matrix-interactions` viewer refreshes automatically on the next\n  `pnpm dev` / build (the generator re-inlines this file). No manual copy step.\n"
