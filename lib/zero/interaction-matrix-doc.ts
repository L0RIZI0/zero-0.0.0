/**
 * AUTO-GENERATED — DO NOT EDIT BY HAND.
 * Source of truth: docs/interaction-matrix.md
 * Regenerated by scripts/gen-interaction-matrix.mjs via the predev / prebuild hooks.
 *
 * This file inlines the canonical markdown so the /matrix-interactions web viewer can
 * render it (the page is a static/client component with no runtime fs). Edit the .md,
 * not this file — it will be overwritten on the next dev/build.
 */
export const INTERACTION_MATRIX_MD = "# Zero — interaction matrix (what-triggers-what)\n\nThe single place that answers \"when the user does X to a [kind] entity in [state], what\nhappens (and what's the guard)?\" Use it to check a NEW request against existing behaviour\nBEFORE building — if a request contradicts a CURRENT row, flag it; if it fills a PROPOSED\nrow, cross-check the TODO ledger.\n\nStatus tags: **[CUR]** = built + verified today · **[PART]** = partially built ·\n**[TODO]** = proposed/awaiting decision (see TODO ledger).\n\n> **This is the SOURCE OF TRUTH.** It's a git-tracked repo file. The `/matrix-interactions`\n> web viewer renders it via `lib/zero/interaction-matrix-doc.ts`, which is AUTO-GENERATED from\n> this file by `scripts/gen-interaction-matrix.mjs` (runs on `predev`/`prebuild`). Edit THIS\n> file; never hand-edit the generated `.ts`. The memory doc\n> `v0_memories/user/zero-interaction-matrix.md` is only a pointer here.\n\n## Vocabulary\n- **kinds**: soul · individual · organism · community · space · resource · task · moment · instant.\n- **states** (`getState().word`, one axis, mutually exclusive; kinds.ts): `open` → `complete`\n  → `closed`; plus `ongoing`, `cancelled`, terminal `dead`/`retired`. `open` includes a\n  reopened entity.\n- **axes/fields**: `schedule{startAt,endAt,at,dueAt,duration,engagements[]}` · `closeAt` (frozen\n  close instant) · `closePolicy` ('manual' opts out of auto midnight-close) · `completed` (task done\n  scalar) · `accent` (color) · `taggedContextIds` (multi-parent links) · `createdAt`/`ownerId`/`createdBy`.\n  EFFECTIVE END (`effectiveScheduleEnd`, kinds.ts): declared `endAt`, ELSE concrete `startAt`+`duration`\n  (minutes) ⇒ IMPLIED end. Used everywhere an end matters (getState ongoing/complete, `computeCloseAt`,\n  lifeline bounds `entityInterval`, dayline paint) so start+duration ends at start+duration, never a ghost.\n  Engagements (RENAMED from \"sessions\" v0.4.8) each have `{startAt,endAt?,via?:\"focus\"|\"play\"}` (`via`\n  renamed from \"kind\").\n\n## TRIGGERS\n\n### Navigation\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Drill into (open) an entity | any kind | Records a presence segment (activity log); becomes the breadcrumb leaf | no-op if same id repeated | [CUR] |\n| Drill INTO a web resource | resource w/ `webUrl` (desktop) | mounts a native tab; if already warm/parked ⇒ instant reveal (no reload) | web preview iframes internal `/…`; native only in Electron | [CUR] |\n| Drill AWAY from a web resource | resource w/ `webUrl` (desktop) | PARKS the tab: hidden + background-throttled, kept resident (warm) for instant re-open | LRU pool cap 4 ⇒ oldest parked tab destroyed | [CUR] |\n| Close button (§0/§1 header ×) | any drilled-in entity (`path>1`) | climbs out to parent; for a `webUrl` resource also DESTROYS its tab for good | root has no × | [CUR] |\n| Inline row METAFIELD | any child in ENTITY CONTENT | each row shows a kind-relevant metafield (right-aligned, muted, before the lifecycle token): moment/space → span \"start–end · dur\" / \"since start · dur\" (live) / \"whenever\" / lone \"at\"; instant → its point time; task → \"due …\" when set, ELSE its own start/end span / ongoing / at (a task can be scheduled too, not just due); individual → \"sex · age\"; others → nothing. Time-of-day when same day, else \"MMM D, h:mm\". Derived via `rowMeta(e, now)`, never stored | hidden on narrow (<sm) widths; empty kinds add no gap | [CUR] |\n| Right-click ▸ Hide / Unhide | any child in ENTITY CONTENT | toggles stored `hidden`; row collapses via a long 650ms eased grid-rows slide+fade, siblings renumber | counts/timeline/siblings untouched | [CUR] |\n| Right-click ▸ Show hidden / Hide hidden | current context | VIEW toggle (session-only, resets on nav): reveals hidden + auto-hidden children with a `(hidden)` prefix | not a data mutation | [CUR] |\n| Auto-hide (derived) | closed child | a child closed BEFORE today's 5am day-start collapses from ENTITY CONTENT | computed from `closeAt`, never stored; revealed by Show hidden | [CUR] |\n| PLANNED vs RECORDED rails on the TODAY dayline (OPTION A, v0.4.0; band-grows v0.4.4) | declared/planned span vs recorded engagement | TWO TOUCHING RAILS split at a SEAM, framed PLAN vs REALITY: PLANNED rail (TOP, grows DOWNWARD from band top) = DECLARED spans only (`planned` memo — scheduled occurrences past/future incl. ongoing declared spans); RECORDED rail (BOTTOM, grows downward from the seam) = the viewed entity's RECORDED engagements (`engagements` memo — closed + running). PRESENCE is REMOVED from this lane (now only on the standalone ACTIVITY dayline). Viewing ROOT ⇒ recorded rail == presence (presence = root's engagements). Seam sits at the band's VERTICAL CENTER and the WHOLE band grows as either rail gains sub-lanes so the seam stays centered (`bandMetrics(plannedCount, recordedCount)`, each half = taller rail's content + `RAIL_PAD`): resting seam=15px / band=30px with one sub-lane each | combined lane (`tracks=\"both\"`) only; standalone `tracks=\"presence\"` lane stays one centered band | [CUR] |\n| RECORDED engagement bars (bottom rail) | any entity in the subtree with `schedule.engagements` (incl. \"whenever\" playables) | one bar per engagement touching today, clipped to the day: OPEN (no `endAt`) ⇒ running bar anchored at the now-edge; CLOSED ⇒ completed span. `via` is `play`+`focus`. Puts a Play'd whenever entity on the dayline | walks `collectDescendants(ROOT_ID)` (playables are un-clocked); key prefix `sess:` routes it to the recorded rail; tooltip appends `· play\\|focus [· ongoing]` (from `engagement.via`) | [CUR] |\n| PLANNED bar length from --duration (v0.4.8) | an entity with a concrete start + `duration` but NO declared `endAt` | paints a FIXED-length planned tick spanning start → start+duration (via `effectiveScheduleEnd`) and reads as NOT ongoing once now passes that implied end — instead of a ghost bar stretching to now. Set through the create bar / menu `--duration:` (`90` / `1h30m` / `2h` / `2d`) | duration with NO concrete start stays meta-only (DURATION row); a declared `endAt` still wins | [CUR] |\n| LANE PACKING within a rail | 2+ spans overlapping in the same rail | greedy interval packing (`packLanes`): each bar takes the lowest lane whose last bar ended at/before its start; else a new lane opens. TOP packs by START (tiling declared spans share lane 0); BOTTOM packs LONGEST-FIRST (longest engagement hugs the seam) — this SUBSUMES the old ongoing stack (all running engagements share the now-edge ⇒ fully overlap ⇒ one lane each, longest nearest seam). Overflow (v0.4.4; centered-seam v0.4.5) = the WHOLE BAND GROWS TALLER around a CENTERED seam: ticks keep FIXED height (`PLANNED_LANE_H=11`/`RECORDED_LANE_H=10`, no shrink) and HUG the seam (planned stack up, recorded stack down); band height = 2× the taller half (`bandMetrics`) | DEFERRED (larger §3 sizes): (2) bounded lanes + \"+N\" marker; band-grow may later be tied to Face size | [PART] |\n| Presence tick rounding | a run of contiguous presence segments (one \"session of using Zero\") | only the FIRST tick of a run rounds its LEFT corners and the LAST rounds its RIGHT; interior ticks are fully SQUARE so a session reads as one continuous pill. A gap (app backgrounded) starts a new run; an isolated tick is fully rounded | `roundLeft/roundRight` derived on the FULL ordered segment list (`prev.leftAt===enteredAt` adjacency) so it survives pan/clip; applies on every lane that paints presence | [CUR] |\n| Tick opacity (any dayline) | any tick | PRESENCE ticks fully OPAQUE at all times (the solid record of where I was). PLANNED ticks ALSO full opacity now — the dynamic coverage/faint mapping is DISABLED via `DYNAMIC_PLANNED_OPACITY=false` (v0.3.95; machinery kept for flip-back). Root presence = solid theme-bg chip (near-black dark / near-white light) + grey hairline | root fill themed; no color-muting | [CUR] |\n| PAST planned tick opacity (coverage) [DISABLED v0.3.95] | a real-duration planned occurrence fully in the past (end ≤ now) | WHEN `DYNAMIC_PLANNED_OPACITY` ON: opacity reflects how much it was HONORED — fraction of its window overlapped by recorded PRESENCE at that entity or any descendant, mapped LINEARLY between a 0.15 floor (missed) and 0.90 ceiling (fully honored) = `0.15 + coverage × 0.75`. Hover still snaps to full. Currently OFF ⇒ full opacity | one place at a time so segments don't double-count; clamped [0,1]; `coverage` derived in the planned memo, re-runs on activity-log change | [CUR] |\n| Dayline tick HIGHLIGHT (cross-component) | HOVER an ENTITY CONTENT row | every dayline tick whose `id` matches the hovered row's entity grows to 26px + goes fully opaque (animated). Threaded as `highlightId` into both daylines (TODAY combined + ACTIVITY presence) | HOVER-ONLY — the open context is deliberately NOT highlighted (a persistent pin felt stuck); cleared on mouse-leave | [CUR] |\n| §4 PINS frame source (v0.5.0 REPURPOSE; +pinning v0.5.1; +global-scan/timer v0.5.2; +two-list v0.5.4) | the §4 band | TWO horizontal lists around an `ONGOING` label: `[ pinned-idle chips ] ONGOING [ ongoing chips ]`. RIGHT of the label = everything OWN-ongoing via `getOwnOngoingEntities` (scans ALL entities — v0.5.2 fix; `isOwnOngoing`; pinned-and-ongoing counts as ongoing and sits right). LEFT = PINNED entities (`getStarterPinnedEntities`, pin order) that are NOT currently ongoing (idle quick-start shortcuts). A chip that starts/stops CROSSES the label and is FLIP-animated between lists (see FLIP row). Re-scans on `dataRev` + a 1s `TICK_MS`. `zero0-pins.tsx` (`zero0-pinned.tsx` DELETED) | auto-shown when either list is non-empty; the ONGOING label shows only when ≥1 ongoing; pins persist via `starterPins` in UserItems | [CUR] |\n| §4 PINS chip FLIP move (v0.5.4; tick-gated v0.5.7) | a chip changing list (pinned-idle ⇄ ongoing) or shifting position | a dep-free FLIP (`useFlipRow`): last position stored per ENTITY ID (survives React's unmount+remount reparent between the two lists) → after commit measure new position → invert with a `translate` then play back over `FLIP_MS≈480ms` `cubic-bezier(.22,1,.36,1)`. Transform lives on a WRAPPER so the chip keeps its own bg/border transitions. v0.5.7 FIX: the effect is now GATED on a structural `layoutSig` (ordered ids per list) and BAILS on unchanged sig — so the 1s timer tick / focus change no longer re-fire it. Also clears any in-flight transform BEFORE measuring the settled LAST rect. (Bug: running every render meant a tick landing mid-transition stored the interpolated position as baseline and re-animated the residual → chip \"bounced right a little every second, decaying\") | no enter/exit anim (a brand-new ongoing chip just appears; an ended UNpinned chip just drops); only ≥0.5px moves animate | [CUR] |\n| §4 PINS chip live TIMER (v0.5.2; stable-width v0.5.3) | an ONGOING chip | a muted `tabular-nums` timer, updated each second (`ongoingTimer`): a KNOWN end (declared `endAt`, or concrete start + duration) ⇒ COUNTDOWN `-Nm Ss` to the end; NO known end (open engagement or open-ended running span) ⇒ ELAPSED counting up from the open engagement's punch-in (else the concrete span start). Uses a LOCAL `formatTimer` (NOT shared `formatDuration`) that ALWAYS keeps the seconds slot AND zero-pads every non-day unit to 2 digits (`00s`→`59s`, `05m 00s`) — so within a tier the char count is CONSTANT and the chip width never jitters as it ticks; only a rare tier-boundary crossing (→ new leading unit) changes width, smoothed by the FLIP row anim; day-scale drops to `Nd HHh MMm` | idle/pinned-not-ongoing chips show no timer | [CUR] |\n| Click a §4 PINS chip BODY (v0.5.6) | ONGOING chip | FOCUS it — `navigateTo` via `openPin`, no engagement write | navigation only (the running engagement/span is untouched) | [CUR] |\n| Click a §4 PINS chip BODY (v0.5.6) | pinned-IDLE chip | START an engagement AND focus it — `openEngagement(id,\"play\")` + `navigateTo` via `startPin(id,true)` | `via:\"play\"` = DELIBERATE + PERSISTENT (not in `focusOpenRef`, so leaving never punches it out; hydrate keeps it running) | [CUR] |\n| ALT/⌘/CTRL-click a §4 PINS chip BODY (v0.5.6) | any chip | START in the BACKGROUND, staying on the current canvas (parallel with anything already ongoing) — `startPin(id,false)`; NO-OP if already ongoing | Alt is the safe modifier (⌘/Ctrl+click = right-click on some platforms); handled in `PinChip.bodyClick` | [CUR] |\n| Click a §4 PINS chip GLYPH (v0.5.6) | any chip | STAY on canvas: ONGOING ⇒ STOP it (`endOngoing` via `endPin` — close its engagement or cap its running span at `now`); IDLE ⇒ START in the background (`startPin(id,false)` = `openEngagement(id,\"play\")`). Glyph SPINS only while ongoing | `stopPropagation` so it doesn't also focus; an ended unpinned chip drops on next scan, an ended PINNED chip lingers (idle) | [CUR] |\n| Right-click a §4 PINS chip | any chip | opens the unified entity menu (`openMenu`) with Pin/Unpin at the top | same menu-model as everywhere | [CUR] |\n| \"Pin\" / \"Unpin\" menu item (ACTIVE v0.5.1) | any entity | top-level toggle (label reflects `isStarterPinned`) that adds/removes the entity from `starterPins` (`toggleStarterPin`), so its chip LINGERS in §4 even when not ongoing. Was the dormant \"Pin as starter\"; RELABELED to plain Pin/Unpin, action ids keep the legacy `starter-` prefix | `starter-pin`/`starter-unpin` canvas-intercepted (not `applyEntityMenuAction`); `openMenu` always passes `starterPinned` so it shows on every entity menu | [CUR] |\n| Engagement storage (all entities) | any entity's clock | ENGAGEMENTS live ON the entity as `schedule.engagements: {startAt, endAt?, via}[]` (RENAMED from `sessions`/`kind` v0.4.8) — the last entry lacking `endAt` is the single OPEN (RUNNING) one. These are the RECORD (what actually happened) and are DELIBERATELY NOT mirrored into scalar `startAt/endAt` (those stay the DECLARED plan — e.g. a Play'd \"whenever\" keeps `startAt:\"whenever\"`) | `getEngagements`/`getOpenEngagement`/`hasOpenEngagement` (pure, kinds.ts); `openEngagement`/`closeEngagement`/`toggleEngagement`/`endOngoing` (writes, data.ts) | [CUR] |\n| Engagement single-instance rule | any entity | at most ONE open engagement at a time — `openEngagement` is idempotent (no-op if one is already open) | — | [CUR] |\n| §4 PINS chip tint (fill v0.5.3; focus-def fix v0.5.5; gentled v0.5.6) | any chip | the entity's own `accent`, else inherited accent from the parent chain, else the bluey Sleep default for sleep-titled, else `--muted-foreground`. Colors the chip border + glyph + a faint accent WASH behind the chip: ~10% `color-mix` normally, a touch STRONGER ~26% when FOCUSED. v0.5.6 REVERTED the v0.5.4 heavy 42%+2px-ring (too loud) back to this gentle level. FOCUSED = \"the chip's entity IS the current canvas context\" (`e.id === focusId`, `focusId = path[len-1]`; the v0.5.5 fix, kept) | `getInheritedAccent` + `sleepDotColor`; `focusId` from the nav path leaf | [CUR] |\n| §4 PINS auto-visibility (v0.5.0) | any load | the band shows ITSELF whenever the union (ongoing ∪ pinned) is non-empty and HIDES (renders null) when empty — no manual toggle. The footer \"pinned\" toggle was REMOVED | driven by the ongoing scan + pin list | [CUR] |\n| §4 PINS list DIVIDER (v0.5.8) | between the two lists | a full-frame-height hairline (`w-px self-stretch bg-border`, `-my-2` to cancel the band padding so it spans edge-to-edge) separates the pinned-idle list (left) from the `ONGOING` label + ongoing list (right) | shown ONLY when BOTH lists are non-empty (nothing to separate otherwise) | [CUR] |\n| §4 empty override (§4 keybinding) | nothing pinned or ongoing | by default the empty band is hidden; pressing §4 (`frequent` flag → `forceShow`) force-reveals the empty frame with a muted \"nothing pinned or ongoing\" placeholder, so you can confirm the frame toggles even when idle | `frequent: false` default in zero0-chord; the §4 digit still maps to it; footer link gone | [CUR] |\n| Color picker free-text (create-bar `--color` + entity \"Set color\" menu) | any color choice | type a hex (`#8b5a2b`) OR a CSS name (`brown`, `grey`); resolved to hex via the browser parser; invalid text can't commit | shares the swatch `color:<hex>` path | [CUR] |\n| Click the minimized TODAY band (v0.4.6) | TODAY (agenda) frame, minimized | LEFT-click on EMPTY band area (anything that isn't a `[data-barkey]` tick) MAXIMIZES the frame; the title collapses/expands with the grid-rows 0fr↔1fr animation. Tick clicks still open their entity (guarded by the `closest(\"[data-barkey]\")` check) | agenda frame only; right-click still opens the frame menu (minimize/maximize) | [CUR] |\n| Chevron in the maximized TODAY title (v0.4.6) | TODAY (agenda) frame, maximized | a chevron button (top-right of the \"today\" title) MINIMIZES on a single click; the title is ALWAYS MOUNTED and collapses via grid-rows so both directions animate smoothly, `inert` while collapsed | `onToggleMinimize` from canvas flips `minimized.agenda` | [CUR] |\n| LAST FOCUS restored on load (v0.4.6) | app open (Electron + web) | the drill-in `path` persists to `zero:root-path:v1` (renderer localStorage) on every change; on mount it's restored + VALIDATED against the hydrated store (keep ROOT then each still-live id, stop at first deleted) so you resume exactly where you left off | own key, isolated from the entity store/activity log; falls back to root on malformed/absent storage or a fully-deleted chain | [CUR] |\n| Being the focused context | any kind | no immediate state change; a dwell timer starts (see next row) | — | [CUR] |\n| DWELL into a Task (whole active path) | task/undone on the `path` | after `DWELL_MS` (~3s) of staying, `openSession(id, \"focus\")` on EVERY undone Task from root→current — so a subtask's work counts for each ancestor Task. Reads `ongoing` EVERYWHERE (crumb/rows/ticks/activity) purely from `hasOpenSession`. Merely passing through to a deeper context leaves NO session (each nav clears the pending timer) | punch-OUT is immediate on leaving; `MIN_SESSION_MS` discards too-short spans; only tracks what the canvas opened (`focusOpenRef`) | [CUR] |\n| Mark a Task Done while it has an open focus session | task with open session | `closeSession` punches it out (a Done task isn't ongoing) even if it stays on the path | done also gates on task-children (see completeSince) | [CUR] |\n| Mark a Task UNDONE while still inside it | task on the current `path`, no open session | `toggleDone` immediately re-`openSession(id,\"focus\")` so the glyph resumes spinning in place — the dwell effect won't re-fire (path unchanged). Fixes: glyph used to stay a static square until you navigated away and back | only when on the path & no session already open | [CUR] |\n| ONGOING glyph rotation | any entity reading `ongoing` (open engagement, or concrete-started moment/space) | glyph rotates clockwise at a calm `SPIN_MS=2345`ms/turn (v0.4.9; was 2340); angular velocity EASES IN at start (playbackRate ramp over `EASE_MS=650`) and decelerates OUT to the nearest upright at stop, so it never snaps on/off. No opacity change. Same for every kind | Web Animations API in zero0-glyph.tsx; pivots the shape's visual centroid; skipped under prefers-reduced-motion | [CUR] |\n| Glyph click on a PLAYABLE entity (`startAt: \"whenever\"`) | moment/space, not-ended | Play/Stop toggle → `toggleSession(id, \"play\")`; opens/closes a background session with NO navigation. Glyph spins while open. Task glyph stays done-toggle; instant stays one-shot — one glyph = one meaning per kind | `isPlayable(e)` = whenever moment/space; also offered as Play/Stop in the right-click menu | [CUR] |\n\n### Lifecycle (right-click menu / create-bar directives)\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Mark as Done | task, not-ended | sets `completed`; for OWNER also completes + stamps `closeAt` (next local midnight) — BUT only if all `kind===\"task\"` children are complete (else \"done but open\", see below) | only kinds with `hasDoneState` (task); manual closePolicy skips the stamp | [CUR] |\n| Mark Done with a still-open task child | task, done, has undone task-child | reads \"done but open\": keeps the Done checkmark, glyph NOT filled, state word = **`done`** (NEW dedicated word, ranked above ongoing/open; NOT `complete`); does NOT stamp `closeAt`. AUTOCOMPLETES (→ `complete`) the instant every `kind===\"task\"` child completes. Non-task children never gate | `allTaskChildrenComplete` in completeSince; `getStateInner` returns `{word:\"done\"}`; `childBreakdown`/`formatState` handle it | [CUR] |\n| Play / Stop (menu) | playable moment/space (`startAt: \"whenever\"`), not-ended | `openSession`/`closeSession` with kind `play` — same as the glyph toggle. Shown INSTEAD of Start now/End now (a whenever entity has no clock time to stamp) | `isPlayable`; label reflects `hasOpenSession` | [CUR] |\n| Mark as Undone | task, done | clears done + `closeAt` | — | [CUR] |\n| Close / Retire / End | fillsWhenClosed or terminal kinds, not-ended | stamps `closeAt` = now-ish; state ⇒ closed / retired / dead | label by `terminal` meta | [CUR] |\n| Cancel | closeable, not-ended | state ⇒ cancelled | HIDDEN for `individual` | [CUR] |\n| Reopen | any ended | clears close/cancel ⇒ open | only shown when ended | [CUR] |\n| Send / Unsend request | task | toggles `requested` (glyph gains a \"sent\" tail) | task only | [CUR] |\n| Start now / End now | moment, not-ended | stamps `schedule.startAt` / `endAt` = Date.now() | not-ended only; no cross-midnight adjust (simple) | [CUR] |\n| Set to now | instant, not-ended | stamps `schedule.at` = Date.now() | instant only | [CUR] |\n| Set color… (submenu) | any kind | `setEntityAccent(id, hex\\|null)`; paints ticks + row | Clear returns to inherited/neutral | [CUR] |\n| Change into… | creatable kinds | `changeEntityKind`; may stamp `closeAt` if new kind time-closes | identity kinds excluded | [CUR] |\n| Delete | any user/seed | removes (user) or tombstones (seed) | — | [CUR] |\n\n### Time / automatic\n| Trigger | Applies to | Effect | Guard / unless | Status |\n|---|---|---|---|---|\n| Reaching `closeAt` | completed task / ended-end moment | state flips complete ⇒ closed at the frozen instant | `closePolicy:manual` ⇒ never (rests complete/ongoing) | [CUR] |\n| Past start, no end | moment OR space with CONCRETE startAt | state ⇒ `ongoing`; glyph spins (pivots true centroid). Space now joins Moment (a live container) | end set ⇒ leaves ongoing; `\"whenever\"` is NOT concrete ⇒ stays `open` until played | [CUR] |\n| Open session present | any entity | state ⇒ `ongoing` purely from `hasOpenSession` ��� highest-priority ongoing branch, view-independent (covers Task focus AND Whenever play) | closes when the session closes | [CUR] |\n| CLOSING an entity ends its open engagement (v0.4.2) | any entity with an OPEN engagement that gets Done / Cancelled / Closed | the close writers (`setEntityCompleted`/`setEntityCancelled`/`setEntityClosed`) call `closeSession(id, now)` on transition-to-closed — the engagement's `endAt` is stamped at the close moment (RECORD side, NOT declared `endAt`), so the bar stops printing as ongoing AND opening the entity shows WHEN it ended. HYDRATE cleanup closes pre-existing ORPHAN engagements on any already-closed entity (`isClosed`, any kind incl. `play`), ending at `lastLogAt` | skipped on un-check/restore/reopen (a reopened entity may run again); sub-`MIN_SESSION_MS` spans dropped. Distinct from the focus-cleanup that keeps a `play` stopwatch running while the entity is still OPEN | [CUR] |\n| CLOSED occurrence, no declared end — dayline clamp (v0.4.3) | a CLOSED Moment/Space with a `startAt` but no `endAt` and NO engagement (so v0.4.2's `closeSession` can't reach it) | the `planned` memo no longer marks a CLOSED occurrence `ongoing`; lacking an `endAt` it terminates the bar at `computeCloseAt` (the RECORD close time) instead of stretching to now. A close time before the visible window ⇒ the bar simply doesn't render (correct — it ended earlier). SECOND, independent ghost-bar source alongside v0.4.2 (engagement-less) | `isClosed(occ)` + `computeCloseAt(occ)` in the memo; declared `endAt` still wins when present | [CUR] |\n| ONGOING ROLLUP (v0.4.0; STOPS-AT-BEING v0.4.9) | any CONTAINER (task/space/moment/instant) with a CONTAINED descendant that is ongoing | state ⇒ `ongoing` (lowest-priority ongoing branch, below own open-engagement / own concrete-span): a Space spins while anything CONTAINED inside it runs. Follows `parentId` CONTAINMENT ONLY — a merely TAGGED-in ongoing entity does NOT light up its tagger. Recurses the subtree, short-circuits on first ongoing descendant (`containedOngoingSince` + `_containedResolver`, separate from `getChildren`) | STOPS AT THE FIRST BEING (`isBeing`: soul/individual/organism/community): a being is never made ongoing by what it contains — it reads ALIVE/present, not \"in progress\". Fixes the v0.4.0 `parentId!==null` guard, which failed for the root Individual (whose parent is the Soul, so it wasn't excluded and wrongly spun whenever ANY descendant ran). Complete/done/cancelled keep their word (rollup ranks below them) | [CUR] |\n| Reload with a dangling session | any entity | hydrate closes any open FOCUS session at the entity's last log stamp (dropping if ≤ `MIN_SESSION_MS`) — focus time shouldn't accrue while the app is closed. PLAY stopwatches (moment/space) are LEFT RUNNING | `[DECISION BAKED]` in `hydrateFromStorage`; easy to flip | [CUR] |\n| Bare `HHMM` time entry | create-bar | resolved into the 5am→5am logical day-window containing now | 6/10-digit tokens keep absolute date | [CUR] |\n\n### Create-bar grammar (see zero-create-field.md for full detail)\n| Trigger | Effect | Status |\n|---|---|---|\n| No `--`/kind hint | default kind = **moment**; title verbatim (no verb inference) | [CUR] |\n| `--due` / `--start`/`--end` / `--at` | kind ⇒ task / moment / instant | [CUR] |\n| `--start:now` etc. | stamps Date.now() | [CUR] |\n| `--end: 5min ago` / `--start: in 2h` | RELATIVE offset from now — `<dur> ago` (past) / `in <dur>` (future); `<dur>` = full duration grammar incl. word units (`5min`, `2 hours`, `1h30m`). Captured WHOLE despite the space (dedicated parseEntry passes before the single-token pass). E.g. ends an ongoing entity at now−5min | [CUR] |\n| `--start:whenever` | marks the entity PLAYABLE — `startAt: \"whenever\"` (a trackable thing with no fixed clock time; glyph/menu offer Play/Stop). Only valid on `--start` | [CUR] |\n| `--<digits>-<digits>` | span shortcut (= `--start:X --end:Y`; date-only end ⇒ +2359) | [CUR] |\n| cross-midnight span | moment `endAt<=startAt` ⇒ end +24h | [CUR] |\n| title contains another entity's title | auto-tag via `taggedContextIds` (+inherit its accent unless `--color`) | [CUR] |\n| `--close:manual\\|auto` | sets `closePolicy` (owner-only) | [CUR] |\n| `--duration:<len>` | sets explicit `schedule.duration` (MINUTES) on ANY kind, independent of start. Accepts `1h30m` / `90` (bare = min) / `2h` / `45s` / `2d` / `1:30`; empty clears. Does NOT change inferred kind | [CUR] |\n\n### Display / derived (not stored)\n| Field | Rule | Status |\n|---|---|---|\n| DURATION / AGE (§0 row) | EXPLICIT `schedule.duration` (via `--duration`) wins for ANY kind regardless of start; ELSE: instant ⇒ 0s; HAS SESSIONS ⇒ Σ session spans (open one live to now) — the tracked-time source for a \"whenever\" playable thing; start+end ⇒ span; ongoing (concrete start, no end) ⇒ now−start (live); BEING (individual/organism) with no span/sessions ⇒ now−`createdAt` (AGE); ELSE ⇒ \"—\". A merely-open \"whenever\" moment/space with NO session reads \"—\". Label = **AGE for individual/organism**, else DURATION. Up to THREE adjacent units incl. mo/y | [CUR] |\n| START / END rows (§0; hybrid inline v0.5.8; column-paired v0.5.9; RICH v0.6.0) | shown ALWAYS for MOMENT and SPACE (both span-bearing). HYBRID INLINE: each row LEADS with the SCHEDULED value (or `— (none scheduled)` — keeps the row's PLANNED-intent meaning), then folds in the TRACKED sessions newest→oldest, COLUMN-PAIRED (Nth start above Nth end, same engagement; nbsp-padded monospace). v0.6.0: built from structured `getScheduleCells` (peer to `getFaceMetaRows`, which flattens the same cells to a plain string for block subsets / whole-row title / aggregate). The FULL face renders them RICHLY: CLOSED session cells are FAINT (`text-muted-foreground opacity-70` — v0.6.1 deepened) vs the foreground scheduled prefix; the OPEN session's `ongoing` END cell is NOT faint (full-opacity foreground, v0.6.1) and PULSES (opacity-only `.zero0-pulse`, motion-safe); each cell carries its own seconds-precision `fmt` `title` (per-value hover, replacing the old whole-row tooltip); the row scrolls HORIZONTALLY (`.no-scrollbar`, silent) to reach old sessions, and START/END share a SYNCED scrollLeft (ping-pong-guarded) so columns stay paired while scrolling. Day-aware stamps auto-gain a date next day (`fmtShort`). Instant shows \"at\"; task \"due\"/condensed \"scheduled\"; other kinds only when set | [CUR] |\n| STATE row when ONGOING (§0; v0.6.1) | any span-bearing entity currently ongoing | the FULL face renders a small rotating spinner (`animate-spin` SVG) + the PULSING word `ongoing` (`.zero0-pulse`), then \" · since …\" plain. Detected by the `ongoing` prefix `formatState` emits; every other state renders as the plain string row | motion-safe (pulse honors reduced-motion) | [CUR] |\n| Dayline planned tick | ongoing entity (startAt, no endAt) draws start→now (openEnded, as if end=now) until a real end exists; label \"· ongoing\" | [CUR] |\n| Dayline SESSION coalescing (v0.5.9) | an entity's `schedule.engagements` on the recorded rail | DISPLAY-ONLY merge: sessions walked oldest→newest, any whose gap from the current run's end ≤ `SESSION_MERGE_GAP_MS` (60s) fold into ONE bar (an open session extends the run to now + seals it). Keeps a burst of quick stop→restart toggles (tests/mis-clicks) reading as a single continuous block instead of fragmenting the rail into extra sub-lanes; a merged bar's tooltip appends `· N sessions`. STORED engagements untouched (§0 lists them all) | genuinely separate sessions (>60s gap) stay distinct bars; presence rail unaffected | [CUR] |\n| Dayline UNKNOWN-END FADE (v0.5.1; shortened v0.5.3) | a PLANNED tick with a concrete start, NO declared/implied end, not closed (`unknownEnd`) | a SHORT fixed-width (`UNKNOWN_END_FADE_PX=20`, was 44) clean linear `accent 0% → transparent 100%` gradient trails RIGHTWARD off the tick's right edge = \"continues, end unknown\". ONGOING ⇒ right edge is the now marker, so the fade spills PAST now into the future; FUTURE open-ended ⇒ start renders as a point and the fade trails right from it. Flat accent (never the sky gradient), non-interactive (keeps the tick's hover), re-anchors top with the tick as lanes restack | a closed tick, a real instant/point, or any bar with an effective end never fades; recorded/session rails unaffected (planned-only) | [CUR] |\n| Dayline tick vertical align | single-track lane ⇒ ticks centered (top:50%). COMBINED lane ⇒ two TOUCHING rails split at a CENTERED, DYNAMIC seam (`bandMetrics`: seam at the band's vertical middle, WHOLE band grows so the seam stays centered). Ticks HUG THE SEAM: planned lane 0 bottom-aligned just above it (higher lanes stack UP), recorded lane 0 top-aligned just below it (higher lanes stack DOWN), FIXED heights, via `-translate-y-1/2` off a numeric `railTop`. The day label centers across the whole top half `[0, seam]` (symmetric margins) and stays anchored as the band grows | [CUR] |\n| Website content zoom (desktop) | trackpad pinch OR ⌘/Ctrl + scroll OR ⌘/Ctrl + = / - / 0 over an embedded web resource | steps that resource's `WebContentsView` zoom factor (0.3–5×, 0.1 steps; 0 resets to 100%). Per-tab, so each resource keeps its own scale. Electron-only (`zoom-changed` + `before-input-event` in main.cjs) | [CUR] |\n| Entity-content row color dot | a small dot before the title ONLY when the entity has its OWN `accent` (manual/`--color`/menu). Inherited ancestor color NOT painted | [CUR] |\n| §0 COLOR meta row | shown only when `accent` set; dt cell paints the swatch, value = raw hex | [CUR] |\n| TAGS / TAGGED BY (§0) | forward links + derived reverse (who points here) | [CUR] |\n\n## Maintenance\n- On any change to getState / menu-model / create-parse / canvas behaviour, UPDATE the\n  affected row(s) and bump the Status tag. Add a row for every NEW trigger.\n- Keep effects one-line; link deep detail to the relevant topic doc.\n- After editing, the `/matrix-interactions` viewer refreshes automatically on the next\n  `pnpm dev` / build (the generator re-inlines this file). No manual copy step.\n"
